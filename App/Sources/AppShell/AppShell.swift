import SwiftUI

/// Root app view. Placeholder until Phase 4 wires the real 5-tab shell.
public struct SocialAppRootView: View {
  public init() {}

  public var body: some View {
    Text("Bluesky (Swift) — under construction")
      .padding()
  }
}
